# {{PROJECT}}

This folder holds two repositories.

- `{{CODE_NAME}}/` is the code. Every piece of work is a GitHub issue on it: {{ISSUES}}.
- `{{DOCS_NAME}}/` is all the documentation. It is the single source of truth.

## Before you do anything

1. Read `{{DOCS_NAME}}/README.md`. It says what every folder is.
2. Read `{{DOCS_NAME}}/NOW.md`. It says what is happening today and what to watch out for.
3. Read `{{DOCS_NAME}}/rules/README.md`, then `{{DOCS_NAME}}/rules/how-we-build.md`, `{{DOCS_NAME}}/rules/working-with-the-owner.md` and `{{DOCS_NAME}}/rules/docs/using-the-documentation.md`.

## The rules that never change

- Look it up before you build it. If the documentation has a page for it, it exists. Use it.
- No work is done until the documentation is updated.
- Project facts go in the documentation, not in your private memory.
- Everything else, including how to build, test and commit, is in `{{DOCS_NAME}}/rules/`.

This block holds no facts about the project, only where to find them.
